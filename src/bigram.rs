//! Day-4 placeholder: dense bigram inverted index, adapted from
//! `fff`'s `bigram_filter.rs` for chunk corpora.

#![allow(dead_code)]

/// Placeholder until Day 4.
pub struct BigramIndex;

impl BigramIndex {
    pub fn empty() -> Self {
        BigramIndex
    }
}
